#include <iostream>
#include <cstdlib>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    char equation[1001];
    int i;
    float res = 0;
    cin >> equation;
    for (i = 0 ; equation[i] != '=' ; )
    {
        for (int j = i ; j != '=' && j != '+' && j != '-' ; j++)
        {
            if 
        }
    }


    system("pause");
    return 0;
}