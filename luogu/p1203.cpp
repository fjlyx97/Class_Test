#include <iostream>
#include <cstring>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int N;
    char pearl[1024];
    cin >> N >> pearl;
    int len = strlen(pearl);    
    int res;
    int max_res = 0;

    for ( int i = 0 ; i < len ; i++)
    {
        res = 0;
        if (pearl[i] != 'w')
        {

        }
    }
    system("pause");
    return 0;
}