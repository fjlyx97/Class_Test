#include <stdio.h>
#include <stdlib.h>
int main(void)
{
    char string[81];
    scanf("%s",&string);
    int i;
    for ( i = 0 ; i < 80 ; i++)
    {
        if (string[i] == '#')
    }
    system("pause");
    return 0;
}